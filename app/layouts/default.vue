<!-- Default layout
     - Wraps page content and applies the base background and layout
     - Keep layout simple; inject global navigation/header component
       at the page level if needed. -->
<template>
    <div class="min-h-screen bg-slate-950">
    <slot />
  </div>
</template>